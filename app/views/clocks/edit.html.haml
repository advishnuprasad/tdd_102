/ %h2= 'ADD Clock'

/ = form_tag('#', :method => "GET") do
/   = label_tag(:country, "Clock Name")
/   = text_field_tag(:country)
/   = label_tag(:timezone, "Time Zone")
/   = text_field_tag(:timezone)
/   = submit_tag("Edit")
/ %br
/ %br

/ =link_to "Go Home" , clocks_path

= form_for @clock do |f|
  = f.label :country  
  = f.text_field :country
  = f.label :timezone
  = f.text_field :timezone
  = f.submit